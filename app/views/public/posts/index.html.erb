<!--/MotoTourReview/app/views/public/posts/index.html.erb-->
<div class='container px-5 px-sm-0'>
  <div class='row gx-5'>
    <!-- 左側部分 -->
    <div class='col-md-4 col-xs-4 mt-5 mb-auto'>
      <%= render "public/users/info", user: @user %>
      <%= render 'map_index' %>
    </div>

      <!-- 右側部分 -->
    <div class='col-md-8 my-5 ml-5'>
      <div>
        <%= render 'search' %>
      </div>

      <div class="row d-flex justify-content-between align-items-center">
        <div class="col-md-8 text-start">すべての投稿一覧</div>
        <div class="col-md-4 text-end"><%== pagy_nav(@pagy) %></div>
      </div>

      <div>
        <%= render 'public/users/users_post' %>
      </div>

      <div class="row d-flex align-items-center text-center">
        <%== pagy_nav(@pagy) %>
      </div>

    </div>

  </div>
</div>
